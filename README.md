# Hospital Maps

Android APK build of the Hospital Maps redesign source.

## Build

GitHub Actions builds `app:assembleDebug` with JDK 17 and Gradle 8.9. The APK is published as the `HospitalMaps-debug-apk` Actions artifact.
